# 🍞 CDN Bakery Chat — PWA для iOS

Превращает `chat.cdnbakery.com` в полноэкранное приложение на iPhone/iPad.

---

## Быстрый старт — GitHub Pages (бесплатно)

### 1. Создайте репозиторий
1. Зайдите на [github.com](https://github.com) → **New repository**
2. Имя: `cdnbakery-pwa` (или любое)
3. Отметьте **Public** → **Create repository**

### 2. Загрузите файлы
Загрузите все 5 файлов из этой папки:
- `index.html`
- `manifest.json`
- `sw.js`
- `icon-192.png`
- `icon-512.png`

Через веб-интерфейс: **Add file → Upload files** → перетащите все файлы → **Commit changes**

### 3. Включите GitHub Pages
**Settings → Pages → Source: Deploy from branch → Branch: main → Save**

Через ~1 минуту сайт появится по адресу:
```
https://ВАШ_ЛОГИН.github.io/cdnbakery-pwa/
```

### 4. Установите на iPhone
1. Откройте ссылку выше в **Safari** (не Chrome!)
2. Нажмите кнопку **Поделиться** (квадрат со стрелкой вверх)
3. Прокрутите вниз → **На экран «Домой»**
4. Нажмите **Добавить**

Готово — иконка появится на рабочем столе, приложение открывается без адресной строки Safari.

---

## Как это работает

```
Safari открывает github.io/cdnbakery-pwa/
        ↓
PWA загружается как fullscreen-приложение
        ↓
iframe показывает chat.cdnbakery.com
с принудительным мобильным viewport
        ↓
Если сайт блокирует iframe (X-Frame-Options)
— показывается кнопка открыть в Safari
```

---

## Если сайт не загружается в iframe

Некоторые сайты выставляют заголовок `X-Frame-Options: DENY` — тогда iframe не работает. В этом случае приложение покажет экран с кнопкой, которая откроет сайт напрямую в Safari — иконка на экране Домой при этом остаётся.

---

## Файлы

| Файл | Назначение |
|---|---|
| `index.html` | Основная страница PWA с iframe |
| `manifest.json` | Метаданные приложения (имя, иконка, цвет) |
| `sw.js` | Service Worker — кэширование оболочки |
| `icon-192.png` | Иконка для Android / PWA |
| `icon-512.png` | Иконка высокого разрешения |
